# Auto-Dev Workflow (Claude Max)
# 使用官方 claude-code-action + OAuth Token + Skills

name: 🤖 Auto-Dev

on:
  issues:
    types: [labeled, opened]
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  auto-dev:
    # 觸發條件：Issue 有 auto-dev label 或 comment 包含 @claude
    if: |
      (github.event_name == 'issues' && contains(github.event.issue.labels.*.name, 'auto-dev')) ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment')

    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📚 Clone Skills Repository
        run: |
          echo "📚 正在下載 Claude Software Skills..."
          git clone --depth 1 https://github.com/miles990/claude-software-skills.git /tmp/claude-skills

          # 複製 skills 到工作目錄
          mkdir -p .claude/skills
          cp -r /tmp/claude-skills/* .claude/skills/

          echo "✅ Skills 下載完成"
          echo "📁 可用的 skills："
          ls -la .claude/skills/

      - name: 📋 顯示任務資訊
        run: |
          echo "=========================================="
          echo "🤖 Auto-Dev 任務開始"
          echo "=========================================="
          echo ""
          echo "📌 觸發事件: ${{ github.event_name }}"
          echo "📝 Issue/PR: #${{ github.event.issue.number || github.event.pull_request.number }}"
          echo "👤 觸發者: ${{ github.actor }}"
          echo ""
          echo "📄 任務內容："
          echo "${{ github.event.issue.title || github.event.comment.body || 'N/A' }}"
          echo ""
          echo "=========================================="

      - name: 🤖 Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          # 使用 skills 的自訂提示
          prompt: |
            你是一個專業的軟體開發助手，配備了完整的軟體開發技能庫。

            ## 可用 Skills
            你可以在 .claude/skills/ 目錄找到 47 個軟體開發 skills，包含：
            - software-design/: 架構設計、設計模式、API 設計
            - software-engineering/: 程式碼品質、測試、DevOps
            - development-stacks/: 前端、後端、資料庫
            - programming-languages/: 各種程式語言最佳實踐
            - domain-applications/: 領域應用模式
            - tools-integrations/: 開發工具整合

            每個 skill 目錄都有 SKILL.md 檔案，包含詳細的指南和最佳實踐。
            請根據任務需求，參考相關的 skill 來完成工作。

            ## 任務
            請根據 Issue 或 Comment 的描述完成開發任務。

            ## 輸出要求
            1. 在開始前，說明你將使用哪些 skills
            2. 執行過程中，顯示進度更新
            3. 完成後，建立 PR 並說明改動內容
            4. 如果遇到問題，清楚說明原因和建議

          # 允許更多工具
          allowed_tools: |
            Bash
            Read
            Write
            Edit
            Glob
            Grep

          # 輸出詳細日誌
          claude_args: '--verbose'

      - name: 📊 顯示執行結果
        if: always()
        run: |
          echo "=========================================="
          echo "🏁 Auto-Dev 任務結束"
          echo "=========================================="
          echo ""
          echo "狀態: ${{ steps.claude.outcome }}"
          echo ""
          if [ -n "${{ steps.claude.outputs.result }}" ]; then
            echo "結果："
            echo "${{ steps.claude.outputs.result }}"
          fi
          echo "=========================================="
