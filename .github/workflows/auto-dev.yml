# Auto-Dev Workflow
# 使用 miles990/claude-software-skills 的 reusable workflow

name: 🤖 Auto-Dev

on:
  issues:
    types: [labeled]
  issue_comment:
    types: [created]
  workflow_dispatch:
    inputs:
      goal:
        description: '開發目標'
        required: true
        type: string

concurrency:
  group: auto-dev-${{ github.event.issue.number || github.run_id }}
  cancel-in-progress: false

jobs:
  auto-dev:
    uses: miles990/claude-software-skills/.github/workflows/auto-dev-reusable.yml@main
    with:
      goal: ${{ github.event.inputs.goal || '' }}
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
